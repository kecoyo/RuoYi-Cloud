<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.butler.mapper.ButlerCategoryMapper">

    <resultMap type="ButlerCategory" id="ButlerCategoryResult">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="icon" column="icon"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="deleted" column="deleted"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <result property="accountCount" column="account_count"/>
    </resultMap>

    <sql id="selectCategoryVo">
        SELECT id, user_id, name, icon, sort, status, deleted, create_by, create_time, update_by, update_time, remark
        FROM butler_category
    </sql>

    <select id="selectCategoryList" parameterType="ButlerCategory" resultMap="ButlerCategoryResult">
        <include refid="selectCategoryVo"/>
        <where>
            deleted = 0
            <if test="name != null and name != ''">
                AND name like concat('%', #{name}, '%')
            </if>
            <if test="remark != null and remark != ''">
                AND lower(remark) like concat('%', #{remark}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="params.beginTime != null and params.beginTime != ''"><!-- 开始时间检索 -->
                AND date_format(create_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''"><!-- 结束时间检索 -->
                AND date_format(create_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
            </if>
        </where>
        order by sort, update_time desc
    </select>

    <select id="selectCategoryCountList" resultMap="ButlerCategoryResult">
        select t.id, t.name, t2.cnt as account_count
        from butler_category t
            left join (
                select category_id, count(*) as cnt
                from butler_account
                where deleted = 0
                group by category_id
            ) t2 on t.id = t2.category_id
        where t.deleted = 0
        order by t.sort, t.update_time desc
    </select>
    <select id="selectCategoryById" parameterType="Long" resultMap="ButlerCategoryResult">
        select * from butler_category where id = #{id}
    </select>

    <insert id="insertCategory" parameterType="ButlerCategory">
        insert into butler_category (
        <if test="name != null and name != ''">name,</if>
        <if test="icon != null and icon != ''">icon,</if>
        <if test="sort != null">sort,</if>
        <if test="remark != null">remark,</if>
        <if test="createBy != null and createBy != ''">create_by,</if>
        create_time
        )values(
        <if test="name != null and name != ''">#{name},</if>
        <if test="icon != null and icon != ''">#{icon},</if>
        <if test="sort != null">#{sort},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        <if test="createBy != null and createBy != ''">#{createBy},</if>
        sysdate()
        )
    </insert>

    <update id="updateCategory" parameterType="ButlerCategory">
        update butler_category
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
            <if test="remark != null and remark != ''">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <update id="deleteCategoryById" parameterType="Long">
        update butler_category set deleted = 1, update_time = sysdate() where id = #{id}
    </update>

    <update id="deleteCategoryByIds" parameterType="Long">
        update butler_category set deleted = 1, update_time = sysdate() where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>